upstream invidious {
    server invidious:3000;
}

server {
    server_name ${INVIDIOUS_HOST};
    include /etc/nginx/ext/ssl.conf;

    location ~ ^/(.*)$ {
        set $upstream http://invidious/$1$is_args$args;
        proxy_pass $upstream;
        include /etc/nginx/ext/proxy.conf;
    }
}
