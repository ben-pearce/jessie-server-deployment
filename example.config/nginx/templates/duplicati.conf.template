upstream duplicati {
    server duplicati:8200;
}

server {
    server_name ${DUPLICATI_HOST};
    include /etc/nginx/ext/ssl.conf;

    location ~ ^/(.*)$ {
        set $upstream http://duplicati/$1$is_args$args;
        proxy_pass $upstream;
        include /etc/nginx/ext/proxy.conf;
    }
}
