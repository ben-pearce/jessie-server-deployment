upstream portainer {
    server portainer:9000;
}

server {
    server_name ${PORTAINER_HOST};
    include /etc/nginx/ext/ssl.conf;

    location ~ ^/(.*)$ {
        set $upstream http://portainer/$1$is_args$args;
        proxy_pass $upstream;
        include /etc/nginx/ext/proxy.conf;
    }
}
